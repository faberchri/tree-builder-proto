package clusterer;

import java.util.List;

public interface Factory {
	
	/**
	 * for now we assume nodesToMerge.size() == 2
	 * @param List nodesToMerge
	 * @return
	 */
	public Node createNode(List<Node> nodesToMerge);
}
